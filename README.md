# sterm

一个使用 `xterm.js` + `node-pty` 构建的 Trae 风格浏览器终端。

## 前置要求

- Node.js 18 或更高版本

## 安装

```bash
npm install
```

## 启动

```bash
npm start
```

## 访问

打开浏览器访问 [http://localhost:3000](http://localhost:3000)
